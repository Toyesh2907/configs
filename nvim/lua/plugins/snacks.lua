return {
    {
        "folke/snacks.nvim",
        opts = {
            picker = {
                hidden = true, -- show dotfiles
                ignored = true, -- show .gitignored files
            },
        },
    },
}
